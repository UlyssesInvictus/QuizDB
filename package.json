{
  "name": "QuizDB",
  "version": "1.0.0",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && npm run deploy",
    "deploy": "cp -a client/build/. public/"
  },
  "heroku-run-build-script": true
}
