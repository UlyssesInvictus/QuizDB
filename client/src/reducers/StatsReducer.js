const initialStatsState = {
  showSidebar: false
}

function stats(state = initialStatsState, action) {
  switch (action.type) {
    case "test":
    default:
      return state;
  }
}

export default stats;
